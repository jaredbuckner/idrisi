# Idrisi Python

This is the Python reference implementation of Idrisi.

```bash
pip install -e .
python -m idrisi.cs --help
./scripts/makemap.sh
```

The scripts write generated maps to the repository-level `out/` directory.
